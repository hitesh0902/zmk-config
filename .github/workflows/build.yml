on: [workflow_dispatch]

jobs:
  build:
    # Reusable workflow used for building custom configurations.
    # If this fails, ensure the input variables (board, shield) 
    # match the schema expected by 'build-user-config.yml'.
